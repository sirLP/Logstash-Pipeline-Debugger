# HAProxy patterns

# HAProxy log format
HAPROXY_DATE %{MONTHDAY}/%{MONTH}/%{YEAR}:%{TIME}\.%{INT}
HAPROXY_TIME %{INT}/%{INT}/%{INT}/%{INT}/%{NOTSPACE}
HAPROXY_BACKEND_NAME %{NOTSPACE}
HAPROXY_SERVER_NAME %{NOTSPACE}
HAPROXY_TERMINATION_STATE [A-Z-]{4}
HAPROXY_HTTP_REQUEST %{WORD:http_method} %{NOTSPACE:http_request} HTTP/%{NUMBER:http_version}

# Status codes
HAPROXY_HTTP_STATUS \d{3}|-1
HAPROXY_QUEUE_BACKEND \d+
HAPROXY_QUEUE_SERVER \d+

# Connections
HAPROXY_ACTCONN \d+
HAPROXY_FECONN \d+
HAPROXY_BECONN \d+
HAPROXY_SRVCONN \d+
HAPROXY_RETRIES \d+
